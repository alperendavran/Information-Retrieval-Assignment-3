# RAG System

This folder contains the main implementation for Assignment 3.

See the root [README](../../README.md) for setup instructions and the [REPORT](../../REPORT.md) for the full assignment report.

## Quick start

```bash
pip install -r requirements.txt
cp .env.example .env   # then add your OPENAI_API_KEY
python main.py --setup
python main.py --interactive
```
